# NEURONS NeuralNS API
PORT=4000
# Comma-separated list of allowed web origins (CORS)
WEB_ORIGIN=http://localhost:3000
# Override the persisted simulation database path (optional)
# NEURONS_DB=./data/neurons.json
